---
template_version: 1
date: 2026-08-28T08:05:45-0600
author: Edgar F. Fuentes Perea
commit: 9d6d8bb
branch: main
repository: frida-code
topic: "Validation of M10 size-app — dimensionamiento cuantitativo de apps para preventa"
status: ready
verdict: fail
parent: ".rpiv/artifacts/plans/2026-08-28_05-31-13_m10-size-app.md"
tags: [validation, frida-size-app, scc, cocomo, frida-extensible-workflows, skill-pack, installer]
last_updated: 2026-08-28T08:05:45-0600
---

## Validation Report: M10 size-app — dimensionamiento cuantitativo de apps para preventa

### Implementation Status

- ✓ Phase 1: Fundaciones del binario — constants + installer — **Fully implemented** (verificada completa, incluidos sus 2 criterios manuales — ver Automated/Manual más abajo)
- ✗ Phase 2: Prompts y resolver 3-capas — **Not implemented** (`skills.ts`, `resolver.ts`, `test/frida-size-app/resolver.test.ts` no existen)
- ✗ Phase 3: Args y validación eager (workflow.ts parte 1) — **Not implemented** (`workflow.ts` no existe)
- ✗ Phase 4: Generador — bootstrap + metrics (workflow.ts parte 2) — **Not implemented**
- ✗ Phase 5: Generador — analyze + synthesize + judge (workflow.ts parte 3) — **Not implemented**
- ✗ Phase 6: Patrón + factory + registro — **Not implemented** (`index.ts` y `pattern.test.ts` ausentes; `src/pi-session.ts` sin entrada — 0 referencias a `createFridaSizeApp`)
- ✗ Phase 7: E2E + dominio COCOMO — **Not implemented** (`e2e.test.ts`, `cocomo-domain.test.ts` ausentes)
- ✗ Phase 8: Doctor + publicación — **Not implemented** (`doctor.ts` sin `checkScc`; `extension.ts` sin entrada HELP_TOOLS ni call-site `agentDir`; `README.md` sin fila; `docs/tools/frida-size-app.md` y `docs/how-to-frida-size-app.md` ausentes; roadmap aún con `scc ⚠️ (bundling por decidir)`)

El árbol de trabajo contiene exactamente los 3 archivos declarados de la Fase 1 (`git status`: 3 sin rastrear del pack + artefactos `.rpiv`); **cero archivos rastreados modificados**. Las casillas del plan coinciden: sólo la Fase 1 tiene `- [x]` (sus 4 criterios automáticos, todos verificados ciertos contra el código).

### Automated Verification Results

- ✓ Typecheck limpio: `npm run typecheck && npm run typecheck:test` — verde, sin errores (src, webview y tests)
- ✓ Tests del installer: `npx vitest run test/frida-size-app/installer.test.ts` — **8/8 casos en verde** (~13 ms), igual que el resto del pack (`test/frida-size-app/` sólo contiene este archivo en el estado actual)
- ✓ Negativo de integridad congela estado inmutable: caso "sha256 incorrecto → rechaza y NO deja nada a medias (V7)" en verde (error matchea `/sha256|integridad/`, sin binario ni marker)
- ✓ Guardián del bump: caso "SCC_DIGESTS cubre exactamente SCC_ASSETS" en verde (8↔8)
- ✓ V8 motor intacto: `git diff --stat src/tools/frida-extensible-workflows/` — vacío (sin cambios al motor)
- ✓ Sin regresiones: cero archivos rastreados modificados (los entregables de la Fase 1 son archivos nuevos); suite hermana de referencia en verde (`test/frida-codebase-index/installer.test.ts` 6/6 — el mold del que parte el pack)
- ✗ Criterios automatizados de Fases 2-8: **no ejecutables** — los archivos objetivo no existen (`workflow.ts`, `index.ts`, wiring de `pi-session.ts`/`doctor.ts`/`extension.ts`)

### Code Review Findings

#### Matches Plan

- `src/tools/frida-size-app/constants.ts` — realización fiel del fence de la Fase 1: `SCC_PIN "4.0.0"`, tabla `SCC_ASSETS` explícita de 8 plataformas con claves Node (`linux-ia32` → `scc_Linux_i386.tar.gz`), `SCC_DIGESTS` con los 8 sha256, helpers `currentSccAsset`/`sccAssetUrl`/`isTarAsset`/`sccBinDir`/`sccBinPath` (win32 → `scc.exe`)/`sccMarkerPath`, `SIZE_APP_FACTORY_NAME = "frida-size-app"`, y los comentarios de disciplina de bump deliberado (molde `CODEBASE_INDEX_PIN`).
- `src/tools/frida-size-app/installer.ts` — fiel al plan: `SccInstallError` con `guide` accionable; `readSccMarker` tolerante a corrupto; `isSccInstalledAtPin` síncrona (marker al pin + asset de la plataforma + binario presente); `extractTarGz` con guard anti tar-slip, GNU LongName (`L`), pax ignorado (`x`/`g`) y directorios (`5`); `defaultFetchArchive` https-only con redirects sólo-https y límite 256 MiB; `ensureBinary` con el orden sha→extraer→copiar→chmod→marker y limpieza de staging en `finally` (nada a medias).
- `test/frida-size-app/installer.test.ts` — los 8 casos especificados: guardián digests↔assets, idempotencia, instalación tar.gz (darwin-arm64) con chmod + marker + staging limpio, zip win32 con `unzipSync` real, sha256 negativo con estado inmutable, tar-slip, plataforma sin asset (freebsd) con guía, fallo de red con guía.
- Suggestion del plan-review aplicada: el import muerto `SCC_ASSETS` NO está en los imports de `installer.ts` (resolución "applied" del triaje Step 5, reflejada en el fence final del plan).
- Criterio manual 1 de la Fase 1 **verificado durante esta validación**: los 8 digests de `SCC_DIGESTS` coinciden **byte a byte** contra el `checksums.txt` real del release v4.0.0 (descargado en vivo y comparado programáticamente: 8/8, 0 mismatches).
- Criterio manual 2 de la Fase 1 **verificado por inspección**: el JSDoc de `extractTarGz` documenta typeflags L/x/g/5 y la decisión de no validar checksums de header (gzip cubre la integridad del stream).

#### Deviations from Plan

- **Implementación truncada en la Fase 1 de 8** — las Fases 2-8 no se ejecutaron: faltan `skills.ts`, `resolver.ts`, `workflow.ts` (3 partes), `index.ts`, el wiring de `pi-session.ts`/`doctor.ts`/`extension.ts`, los tests `resolver`/`pattern`/`e2e`/`cocomo-domain`, la descongelación del doctor y toda la publicación (README, docs par, roadmap). Este es el gap que fuerza `verdict: fail`; no es un defecto de lo implementado sino alcance pendiente.
- `installer.ts` `extractTarGz`: `let fullName` (fence del plan) → `const fullName` en el archivo real — equivalente (nunca se reasigna); variación trivial, no funcional.

#### Pattern Conformance

- ✓ Estructura y tono espejan el molde declarado `frida-codebase-index/constants.ts` + `installer.test.ts`: headers con issue y decisiones (D-numbers), disciplina de pin documentada, deps inyectables sin red, fixtures en tmpdir con `mkdtempSync`/`rmSync` en `beforeEach`/`afterEach`, describe con el formato "`pack · tema (#issue)`".
- ✓ La importación de `unzipSync` desde `../frida-cc-plugins/fetch` reusa la primitiva existente en lugar de duplicar (como exige el plan); el zip de test ejercita el `unzipSync` REAL.
- Minor observation (variación aceptable, no desviación): `installer.test.ts` pasó por el reformateo del linter (pi-lens lo reportó) — p. ej. el cast de `fixtureSccAtPin` envuelto en paréntesis multilínea; cosmético.

#### Potential Issues

- Ninguno en lo implementado. El único riesgo operativo de correr la Fase 1 aislada es nulo: son archivos nuevos sin wiring (nada los importa fuera de su propio test), así que el árbol queda estable y publicable como avance parcial.

### Manual Testing Required

Criterios manuales de la Fase 1 — **completados durante esta validación** (no requieren acción del usuario):

1. Digests vs `checksums.txt` del release v4.0.0:
   - [x] Verificado byte a byte (8/8) contra `https://github.com/boyter/scc/releases/download/v4.0.0/checksums.txt` descargado en vivo.
2. Documentación de `extractTarGz`:
   - [x] Verificado por inspección del JSDoc (typeflags L/x/g/5; sin checksum de header — gzip cubre el stream).

Criterios manuales de las Fases 2-8 (pendientes hasta que la implementación avance — lista de recordatorio, no accionables hoy):

1. V1 — smoke real de scc v4.0.0 sobre OFBiz ANTES de congelar los mocks del e2e (Fase 7, paso previo obligatorio; transcript adjunto a la validación futura).
2. V10 — piloto OFBiz con números de evidencia en el issue #139 (criterio "done" del plan).
3. Doctor UI ("Verificar entorno") con 8 checks incluido scc (Fase 8).
4. Corrida interactiva del patrón en un repo real (pregunta de presupuesto pre-launch → entregables → checkpoint si `review=manual`) — ejercicio del Desired End State.
5. `bash -n` sobre los mocks bash del e2e durante el desarrollo de la Fase 7.

### Recommendations

- Continuar la ejecución del plan por la **Fase 2** (`skills.ts` + `resolver.ts` + tests) — el plan es ejecutable tal cual; la Fase 1 quedó sólida y no necesita re-trabajo.
- Marcar en el plan los 2 criterios manuales de la Fase 1 como completados (ambos verificados aquí: digests byte a byte contra el release real y documentación de `extractTarGz`).
- Re-ejecutar `/skill:validate` sobre el mismo plan cuando las Fases 2-8 aterricen; el veredicto `fail` de hoy se debe exclusivamente al alcance pendiente, no a defectos de lo entregado.
