---
date: 2026-08-29T18:38:48-0600
author: Edgar F. Fuentes Perea
commit: 3002bae
branch: main
repository: frida-code
topic: "Validation of M3 frida-sonar — quality gate local con veredicto por familias, diff por turno y tendencia"
status: ready
verdict: fail
parent: ".rpiv/artifacts/plans/2026-08-29_17-50-18_m3-frida-sonar.md"
tags: [validation, frida-sonar, settings-hub, pi-lens, quality-gate, turn-end, snapshot]
last_updated: 2026-08-29T18:38:48-0600
blockers:
  [
    { id: b1, command: "npx vitest run test/sonar-tab.test.ts", file: "test/sonar-tab.test.ts", line: 1 },
    { id: b2, command: "grep -c \"onLensDiagnostics: lensAndSonar\" src/extension.ts", file: "src/extension.ts", line: 667 },
    { id: b3, command: "grep -ci \"sonar\" webview/components/SettingsHub.tsx", file: "webview/components/SettingsHub.tsx", line: 1 },
  ]
---

# Validation Report: M3 frida-sonar — quality gate local con veredicto por familias, diff por turno y tendencia

## Implementation Status

Implementación **parcial: Fases 1–2 de 6** (coincide con los checkmarks del plan — F1/F2 automated `[x]`, F3–F6 sin marcar). El seam punta a punta está listo; el flujo por turno, el tab, el badge y el gate completo no aterrizaron.

- ✓ Phase 1: Lib pura src/sonar/* — veredicto, diff, consolidación y snapshot — **Fully implemented**
- ✓ Phase 2: Seam completo #126 — settings + mensajes + reducer + case — **Fully implemented**
- ✗ Phase 3: Snapshot por turno — consolidador del bus + hook turn_end — **Not implemented** (`onLensDiagnostics: lensAndSonar` ×0, `scheduleSonarTurnSnapshot();` ×0, sin `mode: "all"`; los 3 hits grep de sonar en `src/extension.ts` son comentarios que anuncian las fases futuras — :667 y :3843-3844)
- ✗ Phase 4: Tab «Sonar» del SettingsHub — **Not implemented** (`webview/components/SonarTab.tsx` no existe; `SettingsHub.tsx` ×0 «sonar»; `styles.css` ×0 `^\.sn-`; `docs/webview-ui-styles.md` ×0 `\.sn-`)
- ✗ Phase 5: Badge de gate en la franja del chat — **Not implemented** (`SonarGateBadge.tsx` no existe; `webview/App.tsx` sin cambios en el diff)
- ✗ Phase 6: Gate completo bajo demanda (FR-7) — **Not implemented** (sin `runFullGate`, case `sonar_gate` sólo con la rama `refresh`)

## Automated Verification Results

Fases implementadas (F1–F2), todos en verde:

- ✓ Type checking pasa: `npm run typecheck` — sin errores (src + webview)
- ✓ Typecheck de tests pasa: `npm run typecheck:test` — sin errores
- ✓ Suite de la lib en verde: `npx vitest run test/sonar-lib.test.ts` — 33/33 tests
- ✓ Corolario reducer en verde: `npx vitest run test/webview-store.test.ts` — 15/15 tests
- ✓ Build completo: `npm run build` — OK, incluye `vite build` → `dist-webview/assets/index-BE_OjoeF.js` (fuente + bundle juntos; sólo el warning preexistente de chunk >500 kB)
- ✓ Lib Node-pura: `grep -c "from \"vscode\"" src/sonar/gate.ts src/sonar/snapshot-store.ts` — 0 en ambos
- ✓ Schema congelada: `grep -c "frida-sonar-gate/v1" src/sonar/gate.ts` — 1
- ✓ Seam completo #126: `grep -c "sonar_gate_state" webview/types.ts webview/store.ts src/extension.ts` — 2/1/1 (≥1 en los tres)
- ✓ Settings registrados: `grep -c "frida.sonar" package.json` — 4 (≥3)
- ✓ Getter en vivo definido y consumido: `grep -n "readSonarConfig" src/settings.ts src/extension.ts` — :440 definición; :218, :726, :743 usos
- ✗ Whole-plan: `npx vitest run test/sonar-lib.test.ts test/sonar-tab.test.ts test/webview-store.test.ts` — la suite del tab no existe; en aislado `npx vitest run test/sonar-tab.test.ts` → «No test files found, exiting with code 1»
- ✗ Criterio F3: `grep -c "onLensDiagnostics: lensAndSonar" src/extension.ts` → 0 (esperado 2) — fase no implementada
- ✗ Criterio F4: `grep -ci "sonar" webview/components/SettingsHub.tsx` → 0 (esperado 4) — fase no implementada
- ✓ Sin regresiones: suites y greps de módulos previos intactos; `lens_status` no tocado por el nuevo reducer (verificado por test propio)

## Code Review Findings

### Matches Plan

- `src/sonar/gate.ts` — fence Fase 1 completo: 32 exportaciones (tipos `SonarFamily`/`SonarIssue`/`SonarThresholds`, constantes congeladas `SONAR_GATE_SCHEMA`/`SONAR_FAMILIES`/`BLOCKING_FAMILIES`, `sanitizeDisabledFamilies`, identidad `issueKey`/`normalizeRuleForDedup`, `runnerToFamily`/`familyOf`, consolidación `mergeSonarPayload`/`flattenIssues`/`anyTruncated`/`countByFamily`, `parseLensDetails` lenient (incluye fix del Plan Review #1: `partial?: boolean` + `d.partial === true`), `progressLineFromUpdate`, `computeVerdict` (D3: FAIL por bloqueantes jamás deshabilitables, WARN por warnings efectivas, no-data honesto), `diffIssues`, `buildTurnSnapshot`)
- `src/sonar/snapshot-store.ts` — `snapshotPath` espejo del patrón workflows vía `encodeCwd` (`src/tools/frida-workflow/audit.ts:22`), `loadSnapshot` tolerante (inexistente/corrupto/schema futuro → undefined), `appendEntry` puro con poda FIFO, `saveSnapshot` atómico 0600 no-throw
- `test/sonar-lib.test.ts` — 33 tests: constantes congeladas, identidad, familias, consolidación del bus (full-replace/out-of-order/clean/severity dual/truncado), veredicto (FAIL/WARN/PASS/no-data/exclusión best-effort/degradado), parse lenient, diff, invariante Σ countsPorFamilia == issues.length (40c7d20), AC FIFO literal del FRD, roundtrip 0600 sin `message` (NFR secrets), `progressLineFromUpdate` (marcado Slice 6, ya verde)
- `src/settings.ts:424-449` — `SonarConfig` + `readSonarConfig()` getter en vivo (molde `readGatePatterns`, sin caché; la lib jamás lee settings, D7)
- `package.json:230-249` — 3 claves `frida.sonar.maxWarnings|disabledFamilies|historyLimit` con defaults 0/[]/500 y enum sin bloqueantes
- `webview/types.ts:832-931` — espejo completo (`SonarFamily`…`SonarFullGateState`/`SonarUiState`, incluido el tipo de la Fase 6), campo `sonar?` en `State` (:1158), `InMessage sonar_gate_state` antes de `error`, `OutMessage sonar_gate` antes de `check_environment`
- `webview/store.ts:693-698` — case `sonar_gate_state` replace plano justo tras `lens_status`, sin tocar `App.tsx` (regla #126)
- `test/webview-store.test.ts:155-245` — corolario reducer con narrowing por discriminante (lección M2), replace sin merge, `lensStatus` intacto
- `src/extension.ts:215-275` (imports completos de la lib incluidos los de F3/F6), `:665-758` (estado host + `postSonarState`/`refreshSonarLensLoaded`), `:2774-2777` (re-post en `webview_ready` tras `postProjectMapState`), `:3841-3848` (case `sonar_gate` sólo rama `refresh`, conforme a la distribución de fences fusionados)

### Deviations from Plan

- Fases 3–6 (bloqueantes, ver Implementation Status): `computeTurnSnapshot`/`scheduleSonarTurnSnapshot`/fan-out `lensAndSonar`/hook `turn_end`/cleanup `newSession`-`switchSession` (F3), `SonarTab.tsx` + registro en `SettingsHub` + familia `.sn-*` en `styles.css` + `docs/webview-ui-styles.md` (F4), `SonarGateBadge.tsx` + montaje en `App.tsx` (F5) y `runFullGate` + rama `run_full_gate` (F6) **no aterrizaron**. El plan está al 33% — el estado de checkmarks del propio plan ya lo refleja, pero el Desired End State (badge, tab, gate completo) está lejos de cumplirse.
- `src/extension.ts:729` — **drift introducido durante esta validación**: el autofix de biome (pi-lens) cambió `let sonarComputeChain` → `const sonarComputeChain` (el fence de la Fase 2 declara `let`). Hoy compila porque la Fase 3 (única reasignadora vía `scheduleSonarTurnSnapshot`) no existe, pero romperá typecheck en cuanto aterrice. Revertir a `let` al reanudar la Fase 3.

### Pattern Conformance

- ✓ La lib sigue el molde `src/project-map/lens-project-report.ts` (Node-only, coerción defensiva, sin vscode) — verificado por grep y por estructura de exports
- ✓ `snapshot-store.ts` reusa `encodeCwd` del patrón workflows (`audit.ts`), misma convención de path canónico que M2
- ✓ El seam replica punta a punta el patrón M2 (#126: mensaje al dispatch general, replace plano D4, re-post en `webview_ready`); diff de `webview/store.ts` y `types.ts` espeja los 4 toques del patrón SettingsHub
- Minor (aceptable, no desviación): los 3 archivos nuevos del run (`gate.ts`, `snapshot-store.ts`, `sonar-lib.test.ts`) no pasan `npx prettier --check` (tabulación Prettier pendiente); los trackeados tocados ya fallaban Prettier en HEAD (verificado con stash — deuda preexistente del repo, no del run). El flujo del repo aplica el reformateo en un commit de estilo separado (molde `094c3bc` de M2).

### Potential Issues

- Cambios en el árbol **fuera del write-set del plan**, preexistentes de otra sesión (sin `--baseline` no hay foto de arranque; no se atribuyen a este run ni cuentan como violación): `webview/followup-rules.ts`, `test/followup-rules.test.ts`, `test/devengine/e2e/reporte-{tools,reasoning,multiturn}-devengine.md`, `test/frida-enterprise/e2e/reporte-{tools,embeddings,multiturn,reasoning}*.md`. Al commitear M3, cerciorar que queden fuera de los commits del feature.
- Working tree con `dist-webview` regenerado por el build (criterio F2, legítimo: fuente + bundle juntos).

## Manual Testing Required

Pendientes de las fases ya implementadas (F1–F2):

1. Settings UI nativa de VS Code:
   - [ ] Las 3 claves `frida.sonar.*` aparecen con defaults 0 / [] / 500 (con enum restringido a las 5 familias no bloqueantes)

Pendientes que llegarán con sus fases (F3–F6): FIFO de dos turnos consecutivos en `<globalStorage>/sonar/<encodeCwd>/snapshot.json`, smoke del badge, gate completo ≤5 min, umbrales en vivo, degradación sin pi-lens, clic en issue → `open_file`.

## Recommendations

- Reanudar la implementación con `/skill:implement` sobre el mismo plan (los fences de F3–F6 ya están distribuidos y documentados; F3–F6 no requieren re-plan).
- Revertir `const sonarComputeChain` → `let` (`src/extension.ts:729`) antes/durante la Fase 3.
- Reformateo Prettier de los archivos nuevos de F1 antes del commit (molde `094c3bc` de M2), junto con la reverificación `npx prettier --check src/extension.ts` que exige la Fase 3.
- No commitear aún: el plan está incompleto (2/6 fases) — el commit parcial debería esperar a las fases restantes o hacerse como commit explícito de checkpoint «seam M3 (F1–F2)».
