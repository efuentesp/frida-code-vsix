---
template_version: 1
date: 2026-08-29T19:43:54-0600
author: Edgar F. Fuentes Perea
commit: 4211d9b
branch: main
repository: frida-code
topic: "Validation of M3 frida-sonar — quality gate local con veredicto por familias, diff por turno y tendencia"
status: ready
verdict: pass
parent: ".rpiv/artifacts/plans/2026-08-29_17-50-18_m3-frida-sonar.md"
tags: [validation, frida-sonar, settings-hub, pi-lens, quality-gate, turn-end, snapshot]
last_updated: 2026-08-29T19:43:54-0600
---

# Validation Report: M3 frida-sonar — quality gate local con veredicto por familias, diff por turno y tendencia

## Implementation Status

Implementación **completa: 6 de 6 fases**. Las Fases 1–2 están commiteadas (29173b2 + reformateo a84aa6b, validadas antes en `.rpiv/artifacts/validation/2026-08-29_18-38-48_m3-frida-sonar.md`); las Fases 3–6 están en el árbol de trabajo sin commitear (estado esperado: validate corre antes de commit). Los checkmarks del plan (`- [x]`) coinciden con el código entregado.

- ✓ Phase 1: Lib pura src/sonar/* — Fully implemented (`src/sonar/gate.ts` 535 líneas, `src/sonar/snapshot-store.ts` 88, `test/sonar-lib.test.ts` 758 — 33 tests en verde)
- ✓ Phase 2: Seam completo #126 — Fully implemented (settings + package.json + types/store espejo + reducer + case `sonar_gate` rama `refresh` + re-post en `webview_ready`)
- ✓ Phase 3: Snapshot por turno — Fully implemented (helpers sección (3) tras `mergeLens`, fan-out `lensAndSonar` en ambos wiring points, hook `turn_end`, cleanups en `newSession`/`switchSession`)
- ✓ Phase 4: Tab «Sonar» del SettingsHub — Fully implemented (`SonarTab.tsx` + registro 4-toques en `SettingsHub.tsx` + familia `.sn-*` completa (43 reglas) + docs + suite)
- ✓ Phase 5: Badge de gate en la franja del chat — Fully implemented (`SonarGateBadge.tsx`, montaje en `App.tsx` entre `LensDiagnostics` y `Followups`, reúso `VERDICT_META`)
- ✓ Phase 6: Gate completo bajo demanda — Fully implemented (`runFullGate` con tool viva `mode=full + refreshRunners=all`, 3 guards de generación `busySince !== startedAt`, rama `run_full_gate`, sin `Promise.race`)

### Automated Verification Results

- ✓ Verificación integral whole-plan: `npm run typecheck && npm run typecheck:test && npm run build && npx vitest run test/sonar-lib.test.ts test/sonar-tab.test.ts test/webview-store.test.ts` — todo verde; build incluye `vite build` → `dist-webview/` (783 kB JS / 245 kB CSS); **71/71 tests** (33 lib + 23 tab/badge/gate + 15 store)
- ✓ F1 — Lib Node-pura: `grep -c "from \"vscode\"" src/sonar/gate.ts src/sonar/snapshot-store.ts` → 0 en ambos
- ✓ F1 — Schema congelada: `grep -c "frida-sonar-gate/v1" src/sonar/gate.ts` → 1
- ✓ F2 — Seam #126 en los tres archivos: `grep -c "sonar_gate_state" webview/types.ts webview/store.ts src/extension.ts` → 2/1/2
- ✓ F2 — Settings registrados: `grep -c "frida.sonar" package.json` → 4 (≥ 3)
- ✓ F2 — Getter definido y consumido: `readSonarConfig` en `src/settings.ts:440` + 8 consumos en `src/extension.ts`
- ✓ F3 — Fan-out en ambos constructores: `grep -c "onLensDiagnostics: lensAndSonar" src/extension.ts` → 2 (ensureSession :1591 y switchSession :6061)
- ✓ F3 — Hook único por turno: `grep -c "scheduleSonarTurnSnapshot();" src/extension.ts` → 1 (tras `scheduleWorkspaceRefresh()` en `turn_end`)
- ✓ F3 — Turno cache-only: `mode: "all"` en `:1402` (`computeTurnSnapshot`), sin `refreshRunners`
- ✓ F4 — Tab registrado: `grep -ci "sonar" webview/components/SettingsHub.tsx` → 4 (import + unión + TABS + render)
- ✓ F4 — Familia CSS: `grep -c "^\.sn-" webview/styles.css` → 43 (≥ 20); docs: `grep -c "\.sn-" docs/webview-ui-styles.md` → 18 (≥ 8)
- ✓ F4 — Sin glifos unicode: `grep -cE "[●○◆▲▼▁▂▃▄▅▆▇█]" webview/components/SonarTab.tsx` → 0
- ✓ F4/F5 — Hover auditado en bundle: `sn-issue:hover{background:var(--vscode-list-hoverBackground)}` y `sn-badge:hover{background:var(--vscode-list-hoverBackground)}` presentes en `dist-webview/assets/index-*.css`
- ✓ F5 — Badge montado: `SonarGateBadge` en `App.tsx:36` (import) + `:767` (montaje); sin mapa local (`Record<SonarVerdict` → 0) reusando `export const VERDICT_META` del tab (→ 1)
- ✓ F6 — Acción punta a punta: `run_full_gate` en `src/extension.ts:4075`, `webview/types.ts:1552`, `SonarTab.tsx:257`
- ✓ F6 — Full vía tool viva sin race: `mode: "full", refreshRunners: "all"` en `:1499`; `Promise.race` → 0
- ✓ F6 — Guard de generación: `grep -c "busySince !== startedAt" src/extension.ts` → 3 (onUpdate + éxito + catch)
- ✓ F6 — Progreso ASCII compartido: `progressLineFromUpdate` en gate.ts (1) / extension.ts (4) / test (9); `[█░]` → 0 en los tres
- ✓ F6 — Historial sólo por turno: `grep -c "appendEntry(" src/extension.ts` → 1 (único append en `computeTurnSnapshot`)
- ✓ Guarda dist-bundle-integrity: `npm run build` regeneró `dist-webview/` consistente con las fuentes webview (fuente + bundle viajarán en el mismo commit)
- ✓ No regressions detected

### Code Review Findings

#### Matches Plan

- `src/extension.ts:1318-1549` — sección (3) completa del fence de la Fase 3 (`sonarOnLens`/`lensAndSonar`/`findLensDiagnosticsTool`/`sonarTurnHostFrom`/`computeTurnSnapshot`/`scheduleSonarTurnSnapshot`) insertada tras `mergeLens`, antes de `ensureSession`, exactamente en el ancla que el Plan Review #2 fijó
- `src/extension.ts:1591, :6061` — fan-out `onLensDiagnostics: lensAndSonar` en los DOS wiring points; `mergeLens` sobrevive como llamada interna del fan-out (3 menciones)
- `src/extension.ts:2694` — `scheduleSonarTurnSnapshot()` tras `scheduleWorkspaceRefresh()` en `case "turn_end"` (hook único)
- `src/extension.ts:5942-5949, :6099-6103` — cleanups en `newSession` (junto a `lensAccum.clear()`) y `switchSession` (antes de `postHistory()`): consolidado + fullGate reset + no-data + post, con el comentario del guard de generación verbatim
- `src/extension.ts` (FR-7) — `runFullGate` verbatim del fence: guard busy, `startedAt` de generación, `onUpdate` con guard de corrida obsoleta, veredicto sobre agregados del full SIN `appendEntry`, catch ruidoso, `postSonarState()` final
- `src/extension.ts:4065-4081` — case `sonar_gate` en su forma final (rama `refresh` de la Fase 2 + rama `run_full_gate` añadida por la Fase 6, con el comentario actualizado)
- `src/extension.ts:726-729` — el árbol corrige a `let` las declaraciones de `sonarComputeChain`/`sonarFullGate` que la Fase 2 había commiteado como `const`, quedando fiel al fence del plan (que las declaraba `let` porque ambos se reasignan)
- `webview/App.tsx:36, :762-773` — import + montaje del badge entre `LensDiagnostics` y `Followups` con `onOpen` que hace `setConfigOpen(true)` + `setSettingsTab("sonar")`; #126 respetado (App.tsx no intercepta mensajes nuevos)
- `webview/components/SettingsHub.tsx` — patrón M2 de 4 toques exacto (import, unión `"sonar"` tras `"environment"`, entrada TABS `label: "Sonar", iconName: "verified"`, render condicional)
- `webview/components/SonarGateBadge.tsx` — verbatim del fence: null en todo estado no-ready, `title` con conteos+diff+causas+hint (sin `message`, NFR secrets), `aria-label` accesible, reúso de `VERDICT_META`
- `webview/components/SonarTab.tsx` — estructura completa del fence: `FAMILY_ORDER`/`FAMILY_LABEL`, `VERDICT_META` exportado, carga al montar vía `sonar_gate refresh`, estados honestos D9 (not-installed SIN botones, no-data/error con Reintentar), chips con `/nd` y `is-off`, issues clicables vía `open_file`, tendencia en barras, `SonarFullGateSection`/`SonarFullGateResult`
- `webview/styles.css` — familia `.sn-*` al final del archivo incluyendo el bloque `.sn-badge` del Slice 5 conforme a la convención de fences fusionados del plan; `:hover` con especificidad (0,2,0) auditada en bundle
- `docs/webview-ui-styles.md` — sección nueva «Tab "Sonar" del SettingsHub (M3 #144) — `.sn-*`» + inventario auditado ampliado preservando la frase «Si agregás un botón nuevo…»
- `test/sonar-tab.test.ts` — 5 describes exactos del fence: F4 (estados honestos + ready), F5 (visibilidad honesta + ready del badge), F6 (gate completo); 23 tests
- `test/sonar-lib.test.ts` — fixtures honestos del contrato 3.8.72 con `partial: true` booleano (fix del Plan Review #1 aplicado) y el AC literal de poda FIFO

#### Deviations from Plan

- `src/extension.ts:1398-1400, :1508-1510` — el `toolCallId` (`frida-sonar-turn-*`/`frida-sonar-full-*`) se extrae a una variable con comentario explicativo («etiqueta del registro de la llamada — molde codebase_index_action») en vez del template literal inline del fence. Mejora menor de legibilidad, semánticamente idéntica — no requiere acción.
- `src/extension.ts:1381-1394, :1521-1534` — la rama `!tool` invierte el orden del ternario del fence (`isLensLoaded() ? error : not-installed` en vez de `!isLensLoaded() ? not-installed : error`). Semánticamente idéntico; con el formateo Prettier del repo queda más legible — variación aceptable, no un gap.

#### Pattern Conformance

- ✓ Lib host pura sigue el molde `src/project-map/lens-project-report.ts` (Node-only sin vscode, coerción defensiva, catch en el caller) y `snapshot-store.ts` el molde `pilens-config.ts` (escritura atómica 0600 no-throw)
- ✓ `SonarTab.tsx` sigue el contrato `{state, post}` y la carga al montar del molde `ProductivityTab.tsx`/`ProjectMapTab.tsx`; es-MX, codicons, variables `--vscode-*`, prefijo per-tab `.sn-`
- ✓ `test/sonar-tab.test.ts` sigue el molde `project-map-tab.test.ts` (`renderToStaticMarkup` + `post=vi.fn()`); `test/webview-store.test.ts` mantiene el narrowing por discriminante (lección M2)
- ✓ Estilo de `src/extension.ts`: el código nuevo usa tabs igual que el archivo (el check de Prettier quedó formalmente «No aplicable» por decisión documentada en el plan; el reformateo completo se hizo aparte en a84aa6b sólo para las fases 1-2)

#### Potential Issues

- Cambios sin commitear AJENOS al plan presentes en el árbol de trabajo: `webview/followup-rules.ts` + `test/followup-rules.test.ts` (reformato de comillas Prettier, 0 menciones a sonar) y reportes e2e regenerados (`test/devengine/e2e/*.md`, `test/frida-enterprise/e2e/*.md`, 3 no rastreados nuevos). No son writes del run de este plan (no tocaron `sonar`) ni afectan verificación alguna; visibles aquí sólo para que el paso de commit los fencee fuera de los commits de M3 (o los commitee aparte como `style:`/`test:`).
- Nota de contexto: no se proveyó `--baseline`/`--scope`, así que el árbol se juzgó completo; los archivos listados arriba son claramente distinguibles del write-set del plan por contenido.

### Manual Testing Required

1. Snapshot por turno (F3):
   - [ ] Dos turnos consecutivos producen dos entradas FIFO en `<globalStorage>/sonar/<encodeCwd>/snapshot.json`, en el mismo orden de cierre
   - [ ] Evento tardío/out-of-order del bus no reemplaza versión con `seq` mayor
   - [ ] Sin pi-lens o con tool caída: el turno termina normal y `sonar_gate_state` muestra `not-installed`/`error` con causa visible
   - [ ] El snapshot persistido no contiene `message` ni contenido fuente (sólo conteos y refs)
2. Tab «Sonar» (F2/F4):
   - [ ] Las 3 claves `frida.sonar.*` en la Settings UI nativa con defaults 0 / [] / 500
   - [ ] Chip «Sonar» (codicon verified) abre el tab; la carga al montar responde sin spinner eterno
   - [ ] Clic en issue `path:line` abre el archivo en esa línea (`case "open_file"`)
   - [ ] Estados honestos: not-installed sin NINGÚN botón; familia deshabilitada atenuada; fría «0/nd» con causa
   - [ ] Tendencia en barras con tooltip por turno
3. Badge (F5):
   - [ ] Turno limpio → panel lens se auto-oculta y el badge SÍ aparece (PASS +0 −0); turno con error → badge FAIL +1
   - [ ] Clic en el badge abre el hub directo en el tab «Sonar»
   - [ ] Sin pi-lens o antes del primer cómputo el badge NO aparece
4. Gate completo (F6):
   - [ ] Botón «Ejecutar gate completo»: progreso ASCII n/m + % con reloj mm:ss, botón deshabilitado, resultado ≤ 5 min
   - [ ] Timeout interno → veredicto parcial «resultados parciales»; familias frías → «no disponible»; el full NO appendea al historial
   - [ ] Doble click no duplica (guard busy); re-monte del tab conserva el reloj (`busySince`); cambio de sesión descarta la corrida obsoleta
   - [ ] Un turno cerrado durante un full en curso coexiste sin romper el estado
5. Umbrales en vivo:
   - [ ] Cambiar `frida.sonar.maxWarnings` 0 → 100 cambia el veredicto en el siguiente cómputo, sin recargar

### Recommendations

- Ready to commit — implementación completa y validada. En `/skill:commit`, agrupar las fases 3-6 del árbol de trabajo con `Refs #144` (fases 1-2 ya viajaron en 29173b2/a84aa6b) y fencear fuera los cambios ajenos (`followup-rules`, reportes e2e) para un commit aparte.
- El smoke manual del badge (turno limpio PASS +0 −0 con panel lens auto-oculto) y el gate completo en vivo son los dos chequeos de mayor valor antes del cierre del issue #144; el resto de la lista manual es de baja rotura (todo el comportamiento honesto ya está cubierto por tests estáticos).
