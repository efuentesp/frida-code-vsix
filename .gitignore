dist/
node_modules/
.DS_Store

# El .vsix es un artefacto de build (30 MB+): se regenera con `npm run package`.
# No se versiona — ralentiza el clone y es multiplataforma por sí mismo.
*.vsix

# Salidas/exploraciones locales de pi (no se versionan).
pi-statusline.txt
pi-provider-litellm.txt
pi-deepseek-usage.txt
catalog-full.txt
